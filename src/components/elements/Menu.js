import React, {PropTypes} from 'react';
import {createThemeElement} from './baseElementUtils';

export const Menu = createThemeElement('menu','aside');

export const MenuLabel = createThemeElement('menu-label');

export const MenuList = createThemeElement('menu-list','ul');
