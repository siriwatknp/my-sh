import { refComponents } from '../utils/componentUtils'
import { background } from '../utils/componentStyles'

export default {
  reference: refComponents.COLUMNS,
  columns: [
    {
      width: 6,
      columns: [],
    },
    {
      width: 6,
      columns: [],
    }
  ],
  background,
  defaultSettings: {},
  deviceSettings: {},
  defaultStyles: {},
  deviceStyles: {},
}
